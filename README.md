# 4-bit-ALU-Verilog
You can use this (under 350 characters):  **"This project implements a 4-Bit Arithmetic Logic Unit (ALU) using Verilog HDL. The ALU performs arithmetic and logical operations such as addition, subtraction, AND, OR, XOR, and NOT based on a selection signal. The design was simulated and verified using EDA Playground."
